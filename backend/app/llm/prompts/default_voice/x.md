Most multi-agent systems are single-agent failures with extra steps.

The token usage data backs this up. Bloated orchestration uses 11x more tokens than a tight single-agent with a skill library. Same output quality. 23x slower.

Context engineering is the unlock. Not more agents.
